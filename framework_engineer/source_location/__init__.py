"""Source-location package for Step 0.5 `locate-kernel-source`.

Currently only the Layer 3 *extract* stage (formerly `import-decomposition`) is
implemented here. The Layer 1 deterministic locator + Layer 2 agent fallback are
future work (see KID_and_locate_source_desgin_v2.md §5).
"""
